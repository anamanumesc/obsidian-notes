**Wireshark** is a graphical tool to analyze network packets.

To simulate a capture with **tcpdump**:
= a **command-line** tool for **capturing** packets directly from a network interface.
`sudo tcpdump -i eth0 -w packets.pcap`
